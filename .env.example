# ============================================
# Healthcare Memory Assistant - Environment
# ============================================

# ----- LLM (OpenAI) -----
# Required for reasoning layer
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_LLM_MODEL=gpt-4o-mini

# ----- Embeddings -----
# Local model, no API key needed
EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2

# ----- Qdrant (Local Docker) -----
QDRANT_HOST=localhost
QDRANT_PORT=6333
# Set to 'true' to force in-memory mode (no Docker needed)
# Set to 'false' for demo with Docker Qdrant (recommended for judges)
QDRANT_MEMORY=false

# ----- Qdrant Cloud (Optional) -----
# Set these instead of HOST/PORT for cloud deployment
# QDRANT_URL=https://your-cluster.qdrant.io
# QDRANT_API_KEY=your-qdrant-api-key

# ----- Application -----
DEBUG=true
LOG_LEVEL=INFO

# ----- Security (Demo) -----
API_KEY_SECRET=demo-secret-key
JWT_SECRET=demo-jwt-secret
